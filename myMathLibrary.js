function addTwoNumbers(num1, num2) {
	var sum = num1 + num2
	return sum
}

function multiplyTwoNumbers(num1, num2) {
	var multiply = num1 * num2
	return multiply
}

module.export.myMathLibrary = {
	addTwoNumbers,
	multiplyTwoNumbers,
}
